Test macros for nRF Connect to use in combination with EST32 BLE server.
Each xml file contains macros to send write data to bluetooth server.
Data are 0x00~0x0C with notification from server to confirm they are received.
